https://acnui5zam93z.feishu.cn/wiki/AwdwwgNwziz8ntkrfWrcT4Lxnyf?sheet=af94bb
https://www.yuque.com/u39080498/eit1gn/zashrgfqd2zy0f0g#LDqU

2026年7月18日
1.Ningbo-2m-new数据集相关代码整理归档，推理，可视化，整理可视化文档。
2.BRIGHT数据集4分类冻结测评（50 epoch, AdamW, lr=1e-4, path_size=4, mIoU=64.53）。
3.CAU_FLOOD数据集相关代码整理归档，推理，可视化，整理可视化文档。

2026年7月20日
1.完成BRIGHT数据4分类在swin-huge权重上的微调测评（50 epoch, AdamW, lr=1e-4, path_size=4, 
backbone_lr=lr*0.1, mIoU=68.12）。
2.BRIGHT数据测评代码归档。
3.BRIGHT数据测评结果可视化文档整理。

2026年7月21日
1.完成SVDT数据集在skysense++模型上的数据加载代码编写。
2.编写SVDT数据集在skysense++模型上的训练配置文件（20 epoch, AdamW, lr=1e-4, path_size=4, 全量微调）。
3.拉起SVDT数据集在skysense++模型上的训练，剩余时间8h。

2026年7月22日
1.与之前复现skysense++的训练配置保持一致，完成SVDT数据在该模型上的训练测评（20 epoch, AdamW, lr=1e-4, path_size=4, 全量微调, mIoU=88.69）。
2.完成Ningbo-2m数据集在skysense++模型上的数据加载代码编写。
3.编写Ningbo-2m数据集在skysense++模型上的训练配置文件（20 epoch, AdamW, lr=1e-4, path_size=4, 全量微调）。
4.拉起Ningbo-2m数据集在skysense++模型上的训练，剩余时间8h。

2026年7月23日
1.与之前复现skysense++的训练配置保持一致，完成Ningbo-2m数据在该模型上的训练测评（20 epoch, AdamW, lr=1e-4, path_size=4, 全量微调, mIoU=72.04）。
2.学习调参配置。

2026年7月24日
1.使用swin-huge权重，对BRIGHT数据集进行微调测评，以原来的微调作为baseline，编写两个新的训练配置文件，核心改动：（配置1），仅将最优模型与早停监控从mIoU变为mFscore；消除“保存mIoU最优权重，而非最优mFscore权重”的目标；（配置2），使用给定的RGB+VV四通道均值/标准差归一化；缩小光学与VV输入图像亮度尺度差异，使双分支微调更稳定。
2.拉起两个配置文件的训练。

2026年7月25日
1.完成BRIGHT数据在（保存最佳mFscore权重；早停patience=10）方案上的微调测评，changed_mFscore=85.77（提 0.41）。
2.完成BRIGHT数据在（保存最佳mFscore权重；早停patience=10；统计训练集的mean与std作为归一化参数）方案上的微调测评，changed_mFscore=85.21（降0.15）。
3.完成BRIGHT数据在（保存最佳mFscore权重；早停patience=10；统计训练集的mean与std作为归一化参数；使用CE权重[1, 1.25]）方案上的微调测评，changed_mFscore=85.23（降0.13）。
4.完成BRIGHT数据在（保存最佳mFscore权重；早停patience=10；统计训练集的mean与std作为归一化参数；新的学习率）方案上的微调测评，changed_mFscore=85.54（提0.18）。
5.将方案3与方案4中的归一化参数去掉，拉起训练，以验证归一化参数对训练是否有负增益。

2026年7月26日
1.使用（保存最佳mFscore权重；早停patience=10；使用CE权重[1, 1.25]）策略对CAU_FLOOD数据进行微调，mIoU=85.13，相比baseline降0.23。
2.使用（保存最佳mFscore权重；早停patience=10；新的学习率）策略对CAU_FLOOD数据进行微调，mIoU=85.72，相比baseline提0.36。
3.统计BRIGHT数据的归一化参数，拉起训练，但被中断。

2026年7月27日
1.使用（保存最佳mFscore权重；早停patience=10；使用CE权重[1, 1.25]）策略对CAU_FLOOD数据进行微调，mIoU=85.13，相比baseline降0.23。
2.使用（保存最佳mFscore权重；早停patience=10；新的学习率）策略对CAU_FLOOD数据进行微调，mIoU=85.72，相比baseline提0.36。
3.统计BRIGHT数据的归一化参数，拉起训练，但被中断。

2026年7月28日
1.千河模型变化检测任务代码迁移，整理，环境配置。
2.调试千河模型在变化检测下游任务里的代码。

2026年7月29日
1.编写CAU_FLOOD数据集在千河模型的变化检测任务中的数据加载。
2.编写CAU_FLOOD数据集在千河模型的变化检测任务中的训练配置文件（仅将数据加载替换为自己写的代码，其余的代码无任何修改）。
3.训练拉起调试，但是会有报错，无法解决。
4.将训练启动命令整理成文档（路径/mnt/ht2-nas2/EO_test/wj1/cmds_for_modelGroup_test.txt），并反馈给模型组，供模型组调试与debug。

2026年7月31日
1.千河模型在Ning2m上进行语义分割任务测评完成（以模型组在potsdam数据上的策略，仅做数据加载适配），miou为66.27。
2.模型组变化检测任务代码的多卡训练适配。
3.重新编写训练配置文件，按照epoch的方式进行训练更新，并完成1组训练配置的训练，最佳changed_fscored=83.88。

2026年8月1日
1.CAU_FLOOD数据调参，完成一组训练配置（50 epoch, AdamW, lr=5e-4, backbone_lr=lr*0.1），changed_F1=85.05。
2.千河模型语义分割任务的全量微调训练适配。
3.Ningbo2m数据调参，完成一组训练配置（40000iters, AdamW, lr=5e-5, backbone_lr=lr*0.1），miou=69.81。
